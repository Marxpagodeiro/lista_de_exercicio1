altura=float(input("Qual a altura do retangulo? "))
base=float(input("Qual a base do retangulo? "))
area= altura*base
perimetro=(2*altura)+(2*base)
print("A area do retangulo é ",area," e o perimetro do retangulo é ",perimetro)
